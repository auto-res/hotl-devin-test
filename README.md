# Base branch for Pruning Research

`tests/new_pruning_method.py` is test code that checks whether the pruning method implemented by Devin is compatible with the existing training codebase of the research group.
The pruning method being developed must pass the test function: `test_pruning_method`.
